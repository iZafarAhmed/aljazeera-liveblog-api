# Al Jazeera LiveBlog API

A serverless API to fetch live updates from Al Jazeera liveblogs.

## Deploy to Vercel

1. Fork/Clone this repository
2. Install Vercel CLI: `npm i -g vercel`
3. Deploy: `vercel --prod`

## Usage

### Get LiveBlog Children IDs
